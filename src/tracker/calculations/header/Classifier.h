#pragma once

#include <windows.h>
#include <vector>

/// <summary>
///		Divides points into classes
/// </summary>
class Classifier
{
public:
	/// <summary>
	///		
	/// </summary>
	/// <param name="centers">
	/// 
	/// </param>
	Classifier(std::vector<POINT> centers);
};

